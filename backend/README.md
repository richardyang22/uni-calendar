### Prisma instructions

Creating and applying a migration
`prisma migrate dev --name <name-of-migration>`

Generating the prisma client
`npx prisma generate`

Viewing/editing the data
`npx prisma studio`